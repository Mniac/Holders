<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zeroc.holders">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".in_game"/>
        <activity android:name=".vista_puntuaciones"/>
        <activity android:name=".options_menu"/>
        <activity android:name=".selection_game"/>
        <activity android:name=".Instalador"/>
        <activity android:name=".TheHolders"/>
    </application>

</manifest>